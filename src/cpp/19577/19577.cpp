#include <iostream>
#include <cmath>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n;
    cin>>n;
    //어떤 임의의 수 x이하의 서로소 개수 * x == n인 최소 x
    //오일러 피 알고리즘은 알고 있음 -> 이로써 이하 서로소 개수는 알 수 있음
}