package kotlin.`17485`

import java.util.Scanner

fun main() {
    val (n, m) = readln().split(" ").map { it.toInt() }
    val arr = Array(n+1) {IntArray(m+1) {Int.MAX_VALUE} }
    val dp = Array(n+1) {IntArray(m+1) {0} }
    val sc = Scanner(System.`in`)
    for(i in 0 until n){
        for(j in 1 .. m) {

        }
    }
}